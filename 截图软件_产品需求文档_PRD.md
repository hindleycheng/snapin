# 截图软件 产品需求文档（PRD）

> 版本：v1.2（决策定稿）
> 编写日期：2026-07-28
> 状态：核心决策已确认
> 产品名：**Snapin（闪贴）**
>
> **关键决策（v1.2）**：① 一次性买断，无内购；② 试用采用**功能限制**（免费版可用，高级功能锁定，激活解锁）；③ **取消云同步**，账号简化为**邮箱 + 授权码激活**，历史记录纯本地；④ 首发核心卖点 = 贴图钉屏；⑤ 品牌 Snapin / 闪贴 + Logo 已定稿；⑥ 分发 = 官网直下。

---

## 1. 产品概述

### 1.1 产品定位
Snapin（闪贴）是一款轻量、专业的**跨平台桌面截图与标注工具**，同时支持 macOS 和 Windows。核心价值是：**快、准、好用**——从按下快捷键到完成一张带标注的截图并分享，全流程无缝、零学习成本。其中"**贴图钉屏**"是首发核心差异化卖点。

面向"每天都要截图沟通"的知识工作者：产品/研发/设计/运营/客服/教师。

### 1.2 一句话卖点
> 一个快捷键，截图、标注、贴图、分享一气呵成。

### 1.3 设计原则
- **极速**：唤起 < 100ms，主流程不打断。
- **专注**：默认界面极简，进阶功能收纳。
- **一致**：Mac 与 Windows 交互与视觉高度统一，同时尊重各平台原生习惯。
- **可扩展**：为后续 OCR、录屏、云同步预留架构空间。

---

## 2. 目标用户与场景

| 用户角色 | 典型场景 | 核心诉求 |
|---------|---------|---------|
| 产品/项目经理 | 标注需求、圈画 Bug、评审沟通 | 快速圈选 + 文字/箭头标注 |
| 研发工程师 | 截报错、贴代码、贴图对比 | 贴图置顶、取色、滚动截长图 |
| 设计师 | 取色、标注间距、截参考图 | 拾色器、像素级选区、尺寸标注 |
| 运营/客服 | 教程配图、快速回复客户 | 一键分享、马赛克打码 |
| 教师/讲师 | 课件配图、录屏演示 | 长截图、录屏（后期） |

### 2.1 关键用户旅程
1. 按下全局快捷键 → 屏幕进入截图模式（暗化遮罩 + 高亮选区）。
2. 拖拽/智能识别窗口 → 确定选区。
3. 弹出工具栏 → 标注（矩形/箭头/文字/马赛克/画笔）。
4. 输出 → 复制到剪贴板 / 保存文件 / 贴到屏幕 / 分享。

---

## 3. 功能需求

### 3.1 核心功能（V1 必做，MVP）

**F1 屏幕截图**
- F1.1 全局快捷键唤起（默认 Mac `⌘⇧A`，Win `Ctrl+Shift+A`，可自定义）。
- F1.2 区域截图：拖拽矩形选区，支持像素级微调（方向键 1px、Shift+方向键 10px）。
- F1.3 智能窗口/控件识别：鼠标悬停自动高亮窗口或 UI 元素边界，单击即选中。
- F1.4 全屏截图 / 指定显示器截图（多屏支持）。
- F1.5 选区尺寸实时显示（W×H 像素）+ 放大镜辅助定位。
- F1.6 延时截图（3s/5s/自定义），用于捕捉菜单等瞬时状态。

**F2 编辑与标注**
- F2.1 形状：矩形、圆/椭圆、直线、箭头。
- F2.2 画笔：自由绘制、高亮荧光笔。
- F2.3 文字：可选字体、字号、颜色、加粗。
- F2.4 序号标记（1、2、3 自动递增，用于步骤说明）。
- F2.5 马赛克 / 高斯模糊（打码隐私信息）。
- F2.6 橡皮擦、撤销/重做（Ctrl+Z / Ctrl+Y）。
- F2.7 工具属性栏：颜色、粗细、透明度可调。

**F3 输出与分享**
- F3.1 复制到剪贴板（默认，`Enter`）。
- F3.2 保存为文件（PNG/JPG，支持自定义路径与命名规则）。
- F3.3 贴图/钉图：将截图作为置顶浮窗贴在屏幕上，可缩放/移动/透明度调节。
- F3.4 取消（`Esc`）。

**F4 基础设置**
- F4.1 快捷键自定义。
- F4.2 保存路径、文件名模板（如 `Snap_{yyyyMMdd_HHmmss}`）。
- F4.3 开机自启、托盘/菜单栏常驻。
- F4.4 图片质量、默认输出格式。

**F5 邮箱账号与授权激活（v1.2 定稿）**
- F5.1 邮箱账号：仅支持**邮箱登录**（用于身份识别与找回授权码），不做密码体系可采用邮箱验证码登录（免密）。第三方登录暂不做。
- F5.2 授权激活：买断购买后，用户获得**邮箱 + 授权码（License Key）**，在软件内输入二者验证通过即解锁正式版。
- F5.3 激活机制：授权码与邮箱绑定，服务端校验一次后本地写入激活凭证，**支持离线使用**（不需常连网）。
- F5.4 设备限制：一个授权码可激活 **2 台设备**，超出时提示；提供在软件内"解绑本机"以便迁移设备。
- F5.5 隐私：仅收集邮箱用于授权与找回，本地截图与历史**全部本地存储，不上传云端**。

> **v1.2 变更说明**：应产品决策，**取消云同步**。账号体系简化为"邮箱 + 授权码激活"，仅服务于身份识别与 License 校验，不承载数据同步。历史记录回归纯本地。

### 3.2 进阶功能（V2 规划）
- P1 **长截图/滚动截图**：自动滚动拼接网页或长文档。
- P2 **OCR 文字识别**：框选区域提取文字，支持中英混排。
- P3 **拾色器（取色）**：屏幕任意像素取色，输出 HEX/RGB。
- P4 **历史记录**：本地截图库，可搜索、重新编辑、批量导出（纯本地，不上传）。
- ~~P5 云同步~~：**已取消**（产品决策，v1.2）。

### 3.3 远期功能（V3+）
- 屏幕录制（GIF/MP4）。
- 团队协作标注、评论。
- 插件/自动化（快捷指令、API）。

### 3.4 功能优先级（MoSCoW，v1.1 调整）
| 优先级 | 功能 |
|-------|------|
| Must | F1 截图、F2 标注、**F3.3 贴图钉屏（核心卖点）**、F3.1/3.2/3.4、F4.1/4.2、**F5 邮箱+授权码激活** |
| Should | F4.3/4.4、P3 拾色器、**P4 历史记录（纯本地）** |
| Could | P1 长截图 |
| Won't（本期） | P2 OCR、录屏、~~云同步（已取消）~~ |

> 说明：贴图为核心卖点，升为 Must；因取消云同步，账号简化为邮箱+授权码激活（服务于 License 校验）；历史记录为纯本地功能。

---

## 4. 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 唤起延迟 < 100ms；截图到剪贴板 < 200ms；空闲内存占用 < 150MB |
| 兼容性 | macOS 12+（Intel/Apple Silicon 通用）；Windows 10/11（x64） |
| 多屏 | 支持多显示器、不同 DPI 缩放、混合分辨率 |
| 稳定性 | 崩溃率 < 0.1%；后台常驻不影响系统性能 |
| 安全隐私 | 截图数据默认本地存储，不上传；打码不可逆；权限透明申请 |
| 可访问性 | 全键盘操作、快捷键可自定义、高对比模式 |
| 国际化 | 首发中/英双语，架构支持多语言扩展 |

### 4.1 平台权限要求
- **macOS**：需申请"屏幕录制"权限（`Screen Recording`），首次使用引导授权。
- **Windows**：默认无需特殊权限；如需全局快捷键与窗口识别，注意 UAC 与多屏 DPI。

---

## 5. 跨平台技术选型建议

推荐两种方案，按团队情况选择：

### 方案 A：Electron + 原生模块（推荐，快速落地）
- **技术栈**：Electron + TypeScript + React（渲染层）+ 原生 Node 插件处理截屏。
- **优点**：一套代码双平台、生态成熟、UI 迭代快、Web 技术栈复用。
- **缺点**：包体较大（~80MB+）、内存占用偏高。需针对性能优化（贴图窗口用轻量渲染）。
- **关键库**：`desktopCapturer`/原生 API 抓屏、`sharp` 图像处理、`robotjs`/原生取色。

### 方案 B：Tauri + Rust（推荐，追求轻量高性能）
- **技术栈**：Tauri（Rust 后端 + 系统 WebView 前端）+ React/Vue。
- **优点**：包体极小（~5-10MB）、内存低、启动快、安全性好。
- **缺点**：Rust 学习成本、部分原生截屏能力需自行封装（`screenshots`/`xcap` 等 crate）。
- **适合**：对性能和体积敏感、团队接受 Rust。

### 方案 C：完全原生（性能最佳，成本最高）
- macOS：Swift + ScreenCaptureKit / AppKit；Windows：C++/C# + Win32/Graphics Capture API。
- **优点**：体验与性能最佳。**缺点**：两套代码、维护成本翻倍、迭代慢。

> **我的建议**：V1 用 **方案 B（Tauri）** 兼顾体积与性能，符合截图软件"轻快"的定位；若团队更熟 Web 且要极速迭代，则选 **方案 A（Electron）**。核心截屏/贴图/取色等系统能力无论哪种方案都需要少量原生代码封装。

---

## 6. 竞品参考

| 产品 | 平台 | 亮点 | 可借鉴点 |
|------|------|------|---------|
| Snipaste | Win/Mac | 贴图强、免费、轻量 | 贴图交互、极简工具栏 |
| Snagit | Win/Mac | 功能全、长截图、录屏 | 编辑器、模板化输出 |
| 微信/QQ 截图 | Win/Mac | 快捷、国民度高 | 快捷键习惯、智能窗口识别 |
| 系统自带（截图/Snipping Tool） | 各自 | 免费、无需安装 | 基础流程标杆 |
| ShareX | Win | 开源、自动化强 | 工作流/上传自动化 |

**差异化机会**：极简高速 + 强贴图 + 中英双语 + 后期 OCR/云同步一体化。

---

## 7. 迭代路线图（v1.1 调整）

| 版本 | 目标 | 主要内容 |
|------|------|---------|
| V1.0 首发 | 核心卖点齐全的截图工具 | F1 截图、F2 标注、**F3.3 贴图钉屏（核心）**、F3 输出、F4 设置、**F5 邮箱+授权码激活**、功能限制试用 |
| V1.1 | 效率与本地资产 | **P4 本地历史记录库**、序号标记、拾色器 |
| V2.0 | 生产力增强 | 长截图 / 滚动截图 |
| V2.5 | 智能化 | OCR 识别、一键分享短链 |
| V3.0 | 平台化 | 录屏、团队协作、API/自动化 |

> 首发即包含"贴图 + 授权激活 + 功能限制试用"，确保买断制闭环与核心卖点同时上线。云同步已取消，数据全本地。

---

## 8. 成功指标（North Star）

- **核心指标**：日均截图次数 / 活跃用户（衡量真实使用频率）。
- 次日/7 日留存率。
- 从唤起到完成截图的平均耗时（体验效率）。
- 贴图、标注功能使用渗透率。

---

## 9. 商业模式与分发（已确认）

### 9.1 定价模式：一次性买断
- **一次性买断，永久使用**，无订阅、无内购。购买后解锁全部功能。
- License 含 **2 台设备授权**（跨 Mac/Win 通用），可在软件内解绑本机以迁移设备。
- **试用策略 = 功能限制**（已确认）：免费版即可下载使用基础截图与标注；**贴图钉屏、历史记录、拾色器、长截图等高级功能锁定**，输入"邮箱 + 授权码"激活后解锁。免费版不限时间、不加水印。
- 收款与发码：官网集成支付（支付宝/微信/信用卡），付款后自动向邮箱发放授权码。

### 9.2 账号与授权（已简化，v1.2）
- **取消云同步**。账号体系简化为：**邮箱登录 + 邮箱&授权码激活**。
- 邮箱用途：身份识别、找回授权码、激活验证。可用邮箱验证码免密登录。
- 激活流程：软件内输入邮箱 + 授权码 → 服务端校验一次 → 本地写入激活凭证 → **之后离线可用**。
- 设备管理：授权码绑定最多 2 台，超限提示；提供"解绑本机"迁移。
- 隐私底线：仅收集邮箱；截图与历史记录**全部本地存储，绝不上传**。
- **服务端最小化**：只需一个"发码 + 激活校验"接口 + 邮件发送，无需数据同步服务，运维成本极低。

> 待确认（见第 11 章）：账号是否需要"在线登录"，还是仅做本地授权码校验（推荐后者，更轻）。

### 9.3 分发渠道：官网直下
- **官网直接下载**（Mac `.dmg` / Win `.exe`），不走 Mac App Store / Microsoft Store（避免抽成与审核限制，也便于自建授权与更新机制）。
- 需自建：官网下载页 + 支付/发码邮件 + 授权校验接口 + 自动更新服务（Mac 用 Sparkle、Win 用 Squirrel/自建 updater）。
- 代码签名与公证：**必须**对 Mac 应用公证（notarization）、Win 应用做数字签名，否则用户下载会被系统拦截告警。

## 10. 品牌（已定稿）

- **英文名**：Snapin　**中文名**：闪贴　（已确认采纳）
- **命名逻辑**：Snap（咔嚓截图）+ Pin（贴图钉屏），连读近 "snap in"（一键贴上），直接点中两大卖点。
- **Slogan**：截了就贴，贴了就用 / Snap it. Pin it.
- **Logo**：已定稿（截图选区框 + 图钉圆点，蓝青渐变）。视觉规范见配套文件 `品牌视觉规范.html`。

## 11. 待确认问题（Open Questions）

1. **账号形态**：推荐"仅本地授权码校验（离线优先）"还是"邮箱在线登录"？（取消云同步后，在线登录用途有限，推荐前者）
2. **哪些功能划入免费版 / 哪些锁定为付费**？初步建议：免费=截图+基础标注；付费=贴图钉屏+历史记录+拾色器+长截图。请确认这个分界。
3. License 大版本升级是否免费（如 v1→v2）？
4. 是否需要中英之外的语言？

---

*本文档 v1.2 已纳入买断、功能限制试用、取消云同步、邮箱+授权码激活等决策，欢迎继续批注迭代。*
